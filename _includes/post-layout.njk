{# 這是一個單篇筆記的版型 #}
<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  {# 頁面標題會動態地變成筆記的標題 #}
  <title>{{ title }} - PHDEV Notebooks</title>
  {# 我們重複使用同一個 CSS 檔案，注意這裡的路徑需要 ../ 返回上一層 #}
  <link rel="stylesheet" href="../css/style.css">
</head>
<body class="post-detail">
  <main>
    <a href="../" class="back-link">← 返回列表</a>
    <article class="post-content-area">
      <h1>{{ icon }} {{ title }}</h1>
      <p class="post-meta">發佈於：{{ date | date('YYYY-MM-DD') }}</p>
      
      {# 這裡可以未來用來顯示筆記的詳細內容 #}
      <hr>
      <p><em>(這裡是筆記詳細內容的顯示區域)</em></p>
      
      {% if tags and tags.length > 0 %}
        <div class="tags-section">
          <strong>標籤：</strong>
          {% for tag in tags %}
            <span class="tag">{{ tag }}</span>
          {% endfor %}
        </div>
      {% endif %}

    </article>
  </main>
</body>
</html>
