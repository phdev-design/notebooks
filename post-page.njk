---
pagination:
  data: posts
  size: 1
  alias: post
permalink: "/posts/{{ post.slug }}/index.html"
---
<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ post.title }} - PHDEV Notebooks</title>
  {# 注意 CSS 路徑，因為這個頁面在子目錄裡，所以需要 ../../ 來返回根目錄 #}
  <link rel="stylesheet" href="../../css/style.css">
</head>
<body class="post-detail">
  <main>
    <a href="/" class="back-link">← 返回列表</a>
    <article class="post-content-area">
      <h1>{{ post.icon }} {{ post.title }}</h1>
      <p class="post-meta">發佈於：{{ post.date | date('YYYY-MM-DD') }}</p>
      
      <hr>
      <p><em>(這裡是筆記詳細內容的顯示區域)</em></p>
      
      {% if post.tags and post.tags.length > 0 %}
        <div class="tags-section">
          <strong>標籤：</strong>
          {% for tag in post.tags %}
            <span class="tag">{{ tag }}</span>
          {% endfor %}
        </div>
      {% endif %}
    </article>
  </main>
</body>
</html>
